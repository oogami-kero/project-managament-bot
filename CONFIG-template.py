import pathlib

#remove the '-template' section from the file name to correctly use this template

BASE_DIR = pathlib.Path(__file__).parent

CMDS_DIR = BASE_DIR / "cmds"

token = 'Place your token here'
